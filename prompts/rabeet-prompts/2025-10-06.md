## Prompts for 2025-10-06

**Prompt:** create a map component using mapbox in MVVM architecture.
make it so the map shows farms and fields from geojson data (passed as prop). farms should be green and fields orange. when i click on a farm, zoom into its bounds and show only its fields. when i click recenter button, go back to initial position and reset filters. also show popups on hover for both farms and fields showing all properties.

**Prompt:** Create a new file in doc folder named as Map Component.md and write the implementation steps in it.
