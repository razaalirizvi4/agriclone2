# Goal
1. **prompt** Build a fully functional Express + MongoDB API for managing dynamic entity types such as Farm, Field, Crop, SoilType, Vehicle, etc.
1. The Type API should support:
1.1 Dynamic attributes
1.2 Child types (optional)
1.3 Assigning typeId to location entities

2. Required APIs
2.1 POST /api/types/ — Create a new Type
2.2 GET /api/types/ — Get all types
2.3 GET /api/types/:id — Get single type by id
2.4 GET /api/types?category=farm — Filter by category
2.5 PUT /api/types/:id — Update a type
2.6 DELETE /api/types/:id — Delete a type

# Your Task:
1. Build Type Model
    Create a MongoDB model using the schema above.

2. Build Type Service
Functions:
    createType
    updateType
    deleteType
    getTypes
    getTypeByCategory

3. Build Type Controller
    Parse request body
    Validate structure
    Call TypeService

4. Build Express Routes
    Route file: /api/routes/typeModule/type.routes.js

5. Integrate With Farm Wizard
    Inside farmSetup() function:
    Fetch Type by ID
    Validate dynamic attributes
    Use Type to dynamically parse request data
    Pass related stage info to event generator

# model should be like this 
## example scehma:
const TypeSchema = new mongoose.Schema({
  type: {
    type: String,
    required: true
  },
  attributes: [
    {
      key: { type: String, required: true },
      valueType: { type: String, required: true }, // string | number | boolean | geojson | array | object
      required: { type: Boolean, default: false }
    }
  ]
});

module.exports = mongoose.model("Type", TypeSchema);
# first write a implementation file in /docs and wait for my confirmation

2. **prompt** the routs are protected with middleware and role middleware

3. **prompt**the controller res should be like this:
return res.json(success(crop));
    } catch (err) {
        return res.status(500).json(fail(err.message));