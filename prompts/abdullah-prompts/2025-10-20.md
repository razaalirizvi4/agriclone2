1. **prompt** there are two fields in a farm when user click on first field the weather, events and crop, should change according to that field same for the second field. what will be the approach of this and first write the approach and implementation steps of this create a new file in docs folder and write the implementation in that file.

2. **prompt** the data, dashboard is currently useing is coming from back-end/mocks

3. **prompt** update the implementation file.