### *prompt* create a new file for the name of crops.model.js which will be defining the mongodb structure for the crops (mock model for now)

### *prompt: * implement the implementtations.md into the app

### *prompt:* push some data in to db

### *prompt:* but it is not updating in compass

### *prompt:* ??? FIrst identify the problem then try to fix if it is database name issue then change the name of crops db to agripro_crops

### *prompt:* hey i wanted to say that a db already is existing with name of agripro we donot need to add a agripro db again we just need to add a crops table inside it so it can use already handling db as its main db as the main db already has location and other tables inside it actually i have imported it from github and didnot excuted them util i didnot executye them they cannot be visible

### *prompt:* cannot we create a seed file like crops.seed.js which can be used to push data without frontend into the mocks folder like the way there is location.seeds.js already present

### *prompt:* you are mixing different table bro we didnot have permissions table you created some table by your own please donot do this

### *prompt:* you created extra tables like permissions

### *prompt:* check again pushing crops.seed.js

### *prompt:* remove extra mock data just include rice corn and wheat in crop.seed.js

### *prompt: * create another field with name of _id in crop.model.js which will be assigning the db autogenerated id to that key and update it in the mock data in the mocks crop.seed.js data

### *prompt: * create the .env file with the port name 5000 for running server so it can be fetched

### 